// Pure HTML renderer. The host may rerender with {complete:false} after a demo button click.
// Demo state never builds, signs or submits a transaction.
const esc=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const link=(title,url)=>`<a href="${esc(url)}" target="_blank" rel="noopener">${esc(title)}</a>`;
export function v4AppsDemoState({complete=true}={}){
 const valid=Boolean(complete);
 return {complete:valid,simulated:true,inputs:['Shop · part 1','Shop · part 2',...(valid?['Shop · part 3']:[]),'Pip · restricted budget'],outputs:valid?['Part 1 → Pip','Part 2 → Pip','Part 3 → Pip','0.05 tKAS → seller','Remaining budget → Pip’s policy']:[],verdict:valid?'Both apps agree. The complete deal can pass.':'The shop rejects the missing part. The deal cannot pass.',explanation:valid?'The shop checks its three assets and payment. Pip’s policy checks the market, recipient, spending cap and remaining budget.':'Pip’s signature alone cannot bypass the shop’s requirement for all three parts.'};
}
export function renderV4AppsExplanation(options={}){
 const state=v4AppsDemoState(options);
 return `<section class="v4-app-composition"><h3>Two apps. One deal.</h3><p>The Supply Shop owns the sale rules. Pip’s wallet owns the spending rules. One transaction must satisfy both.</p><div role="group" aria-label="Illustrate a complete or incomplete deal"><button type="button" data-v4-app-demo="complete" aria-pressed="${state.complete}">Include all three parts</button><button type="button" data-v4-app-demo="incomplete" aria-pressed="${!state.complete}">Leave one part out</button></div><p><small>Illustration only · these buttons spend no coins</small></p><div aria-live="polite"><p><strong>${esc(state.verdict)}</strong></p><p>${esc(state.explanation)}</p><p><strong>Consumed:</strong> ${state.inputs.map(esc).join(' · ')}</p>${state.complete?`<p><strong>Created:</strong> ${state.outputs.map(esc).join(' · ')}</p>`:'<p><strong>Result:</strong> no accepted spend, so no partial transfer.</p>'}</div><details><summary>What the actual purchase uses</summary><p>The Testnet purchase combines two covenant families: three Supply Shop inputs and one Pip budget input. It creates five outputs: three parts owned by Pip, the seller’s payment, and Pip’s remaining restricted budget.</p><p>The node checks both families against the same transaction. The shop cannot waive Pip’s spending cap; Pip cannot waive the shop’s sale conditions. All accounts in this tour belong to your test wallet.</p><p>${link('Read the world contract sources','/assets/v4-contract-source.json')} · Shop: v4-bundle.sil · Pip: v4-agent.sil</p></details><details><summary>Where Argent fits</summary><p>This purchase uses hand-written SilverScript contracts. Argent lets developers express actors and interactions between apps, then generates SilverScript and transaction artifacts. It could make a larger town of compatible apps easier to build; this tour does not execute Argent-generated contracts.</p><p>${link('Argent source and status','https://github.com/argent-lang/argent')} · ${link('SilverScript compiler','https://github.com/kaspanet/silverscript')}</p></details><details><summary>What if an app reports a delivery?</summary><p>Imagine a supplier app signs “the greenhouse panels arrived.” Another app could require that signed claim before paying. The signature identifies who made the claim; it does not establish that the panels arrived. A miner’s signature would have the same limitation.</p><p>This is a possible extension, not a data feed or attestation implemented by this demo. Choosing who may report an outside fact is part of the application’s trust model.</p></details><details><summary>Could a proof keep details private?</summary><p>A proof could show that a private input satisfies a rule without revealing that input, if the application and proof system are designed for it. Public payment amounts, recipients or transaction links may still reveal information.</p><p>This tour produces no ZK proof and provides no transaction privacy. Its scheduling contract checks the small answer directly. ${link('KIP-16 describes the separate ZK verification interface','https://github.com/kaspanet/kips/blob/master/kip-0016.md')}.</p></details><details><summary>Protocol references</summary><p>${link('KIP-10 · transaction introspection','https://github.com/kaspanet/kips/blob/master/kip-0010.md')} · ${link('KIP-17 · covenant scripting','https://github.com/kaspanet/kips/blob/master/kip-0017.md')} · ${link('KIP-20 · covenant identity','https://github.com/kaspanet/kips/blob/master/kip-0020.md')}</p><p>These describe the protocol mechanisms. A specification reference alone does not establish network activation or acceptance of a particular transaction.</p></details></section>`;
}
